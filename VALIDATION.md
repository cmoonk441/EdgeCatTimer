# 검증 기록 — 0.1

2026-09-19 작성.

## 통과한 확인

- Zig 0.13.0으로 Windows x64 GUI 실행 파일 빌드.
- PE 파일이 x86-64 Windows GUI 형식이며 manifest, 아이콘, 버전 리소스를 포함함을 확인.
- Windows 기본 DLL만 참조함을 확인. 별도 Python/.NET/MinGW/Visual C++ 재배포 패키지를 함께 배포하지 않음.
- 실제 구현에서 사용하는 `src/core.hpp`를 Linux C++ 테스트로 실행해 아래 항목 통과:
  - 25분 경과 시간과 진행률 계산.
  - 일시정지 시간 제외 및 재개.
  - 그리기 타이머 호출이 늦어져도 누적 시간이 늘어나지 않음.
  - 완료 처리가 한 번만 발생하며 진행률 100%로 고정.
  - 중단 후 초기 상태 복귀.
  - 네 모서리에서 위치와 회전 방향.
  - 한 바퀴 후 출발 위치·방향 복귀.
  - 음수 좌표를 사용하는 보조 모니터.
  - 10,001개 진행 지점에서 캐릭터가 모니터 내부 경계를 벗어나지 않음.
- 코드가 생성한 기본 고양이 프레임을 이미지로 출력해 확인.

## 직접 실행하지 못한 확인

이 제작 환경은 Linux입니다. Wine을 사용한 실행 확인도 환경의 소켓 생성 제한으로 시작하지 못했습니다. 따라서 아래 항목은 Windows PC에서 아직 검증하지 않았습니다.

- 실제 설정 창의 글꼴·배치와 화면 위 표시.
- 배경 프로그램으로 마우스 클릭 통과, 트레이 아이콘과 단축키.
- 실제 GIF 파일의 프레임 표시와 사용자 이미지 교체.
- 실제 듀얼 모니터의 위치·배율 차이·연결 해제.
- PC 절전·최대 절전 복귀.
- RAM/CPU 사용량과 장시간 실행 안정성.

실행 파일 빌드와 계산 테스트 통과를 실제 Windows 전체 기능 검증으로 간주하지 않습니다.

## 내 PC에서 빠르게 확인하기

1. `EdgeCatTimer.exe` → `10초 시험` → `시작`을 누릅니다.
2. 화면은 그대로 보이고 고양이만 움직이며 약 10초 후 출발점에서 멈추는지 확인합니다.
3. 뒤에 있는 프로그램을 클릭할 수 있는지 확인합니다.
4. 다시 시작하고 Ctrl+Alt+P로 멈춘 뒤 몇 초 기다립니다. 재개했을 때 남은 시간이 그대로 이어지는지 확인합니다.
5. Ctrl+Alt+S로 중단하면 고양이가 숨겨지는지 확인합니다.
6. 보조 모니터를 선택하고 동일하게 확인합니다.
7. 마지막으로 25분을 실행해 작업 관리자에서 CPU와 메모리를 확인합니다.
